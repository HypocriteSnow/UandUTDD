namespace ArknightsLite.Editor.LevelEditor.Core {
    /// <summary>
    /// Workspace persistence will be added on top of this repository as the
    /// workspace-driven editor flow expands in later tasks.
    /// </summary>
    public sealed class LevelEditorWorkspaceRepository {
    }
}
